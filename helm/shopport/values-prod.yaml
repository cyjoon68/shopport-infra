environment: prod
appEnv: prod
app:
  nodeEnv: production
api:
  replicas: 3
  autoscaling: { minReplicas: 3, maxReplicas: 100, cpuUtilization: 60, memoryUtilization: 70 }
worker:
  minReplicas: 1
  maxReplicas: 200
outboxWorker:
  replicas: 2
secrets:
  runtimeRemoteKey: shopport-prod/runtime
  migrationRemoteKey: shopport-prod/migration
  credentialsRemoteKey: shopport-prod/credentials
